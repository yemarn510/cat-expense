# Pawket (Cat Expense)

Monorepo-style layout:

- [`Frontend/`](./Frontend) — React + Vite + Tailwind app
- Playwright E2E tests can live at the repo root (or a future `e2e/` folder)

## Frontend

```bash
cd Frontend
npm install
npm run dev
```

App runs at http://localhost:3001 by default.
